$(document).ready(function () {
  $("#container").html("Hola");

  // Select the input field and change the placeholder attribute
  $("#email").attr("placeholder", "Sorry can't continue your request");
});
