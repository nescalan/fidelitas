CREATE TABLE `pokemon`.`favorites` (
  `id` INT UNSIGNED NOT NULL,
  `name` VARCHAR(150) NOT NULL,
  `type` VARCHAR(150) NOT NULL,
  `ability` VARCHAR(150) NOT NULL,
  `attack` VARCHAR(150) NOT NULL,
  `speed` VARCHAR(150) NOT NULL,
  PRIMARY KEY (`id`));
